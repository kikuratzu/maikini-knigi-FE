/* Address of the Maikini Knigi backend (see /backend).
   Leave empty to use the same host as this page on port 8080, e.g. http://localhost:8080.
   For a deployed site set it explicitly, e.g. "https://api.maikiniknigi.bg". */
window.MAIKINI_API_BASE = "";
